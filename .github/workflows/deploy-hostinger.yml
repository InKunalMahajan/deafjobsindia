name: Deprecated Hostinger Deployment

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  notice:
    runs-on: ubuntu-latest
    steps:
      - name: Hostinger deployment disabled
        run: echo "Hostinger deployment is disabled. DeafJobsIndia now deploys with deploy-pages.yml to GitHub Pages."
